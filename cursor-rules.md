This file is used to configure the behavior of the AI assistant.

- The AI assistant should not modify the `database.py` file.
- The AI assistant should not add new dependencies to the `requirements.txt` file without asking first.
- The AI assistant should not modify the `README.md` file.
