# Exemplo de arquivo de configuração.
# Renomeie este arquivo para .env e preencha com seus valores.

# Chave da API da Groq, necessária para o backend funcionar.
GROQ_API_KEY="gsk_SUA_CHAVE_SECRETA_AQUI"

# URL base da API. Para o ambiente Docker, geralmente é o endereço do proxy.
# Esta variável é usada pelo backend, mas o frontend agora usa caminhos relativos.
API_BASE_URL="http://localhost:8080"

# Porta em que o contêiner do backend (Gunicorn) irá rodar.
# O Nginx irá se conectar a esta porta.
PORT=8080

# Ambiente do Flask. 'production' é recomendado para o deploy.
FLASK_ENV=production
