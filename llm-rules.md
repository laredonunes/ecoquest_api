This file is used to configure the behavior of the LLM.

- The LLM should not suggest the use of new libraries without asking first.
- The LLM should not suggest changes to the database schema.
- The LLM should not suggest changes to the authentication system.
